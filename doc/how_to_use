Type the directory in which the song is or type the song name in
the pop up window(directory contents already listed).

Press return key when done(Not the number Enter key).

The song plays automatically.
